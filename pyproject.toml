[tool.poetry]
name = "xraypy"
version = "0.0.0.9000"
description = "Python tools for astronomy"
authors = ["Grigory Uskov"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.11"
numpy = ">=1.26"
astropy = ">=6.0"
matplotlib = ">=3.8"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
